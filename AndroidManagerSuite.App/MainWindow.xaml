<Window x:Class="AndroidManagerSuite.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:AndroidManagerSuite.App.Controls"
        mc:Ignorable="d"
        Title="Android Manager Suite" Height="800" Width="1180" MinHeight="680" MinWidth="1040"
        Background="#1E1433" Icon="Assets/AppIcon.png"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI Variable Text, Segoe UI">
    <Window.Resources>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#25183D"/>
        <SolidColorBrush x:Key="SurfaceAltBrush" Color="#33204F"/>
        <SolidColorBrush x:Key="BorderBrushSoft" Color="#5C4A7A"/>
        <SolidColorBrush x:Key="TextBrush" Color="#F7F3FF"/>
        <SolidColorBrush x:Key="SubtleTextBrush" Color="#C9BDE0"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#00BCF2"/>
        <SolidColorBrush x:Key="AccentPurpleBrush" Color="#9B72F2"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#FF5C7A"/>

        <!-- Fix selection colors for ListView/DataGrid in Dark Mode -->
        <Style TargetType="ListViewItem">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListViewItem">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderThickness="0" Padding="4">
                            <GridViewRowPresenter Content="{TemplateBinding Content}" Columns="{Binding Path=View.Columns, RelativeSource={RelativeSource AncestorType=ListView}}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="Black"/>
                                <Setter Property="Foreground" Value="#00BCF2"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="Black"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="Padding" Value="14,5"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Foreground" Value="#06111E"/>
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.88"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="ButtonBorder" Property="RenderTransform">
                                    <Setter.Value>
                                        <TranslateTransform Y="1"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="ButtonBorder" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
        </Style>
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource DangerBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource DangerBrush}"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Padding" Value="10,0"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="MinHeight" Value="34"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource SurfaceAltBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
        </Style>
        <Style TargetType="ListView">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="#050505"/>
            <Setter Property="Padding" Value="18,10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#5C2D91" Offset="0"/>
                <GradientStop Color="#68217A" Offset="0.42"/>
                <GradientStop Color="#20103B" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Menu Grid.Row="0" Background="#33000000" Foreground="{StaticResource TextBrush}" Padding="4,4">
            <Menu.Resources>
                <Style TargetType="MenuItem">
                    <Setter Property="Foreground" Value="Black"/>
                </Style>
            </Menu.Resources>
            <MenuItem Header="_File" Foreground="{StaticResource TextBrush}">
                <MenuItem Header="E_xit" Click="ExitMenuItem_OnClick"/>
            </MenuItem>
            <MenuItem Header="_Help" Foreground="{StaticResource TextBrush}">
                <MenuItem Header="_About" Click="AboutMenuItem_OnClick"/>
            </MenuItem>
        </Menu>

        <DockPanel Grid.Row="1" Margin="20">
            <Border DockPanel.Dock="Top" Background="#CC111C2D" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="280"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Border Width="42" Height="42" CornerRadius="10" Background="#F7F3FF" Margin="0,0,12,0">
                            <Image Source="Assets/AppIcon.png" Width="34" Height="34" Stretch="Uniform"/>
                        </Border>
                        <StackPanel>
                            <TextBlock Text="Android Manager Suite" FontSize="20" FontWeight="SemiBold"/>
                            <TextBlock Text="File, app, battery, and performance management over ADB" Foreground="{StaticResource SubtleTextBrush}" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>

                    <TextBlock Grid.Column="2" Text="Device" FontWeight="SemiBold" VerticalAlignment="Center" Margin="24,0,10,0"/>
                    <ComboBox x:Name="DeviceComboBox" Grid.Column="3" DisplayMemberPath="DisplayName" Foreground="#07111F" Background="#EEF7FF" SelectionChanged="DeviceComboBox_OnSelectionChanged"/>
                    <StackPanel Grid.Column="4" Orientation="Horizontal" Margin="12,0,0,0">
                        <Button x:Name="RefreshDevicesButton" Content="Refresh" Click="RefreshDevicesButton_OnClick"/>
                        <Button x:Name="DisconnectButton" Content="Disconnect" Style="{StaticResource SecondaryButton}" Click="DisconnectButton_OnClick" Visibility="Collapsed"/>
                        <Button x:Name="RebootButton" Content="Reboot ▼" Style="{StaticResource SecondaryButton}" Click="RebootButton_OnClick">
                            <Button.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Normal Reboot" Click="RebootNormalMenuItem_OnClick"/>
                                    <MenuItem Header="Reboot to Recovery" Click="RebootRecoveryMenuItem_OnClick"/>
                                    <MenuItem Header="Reboot to Bootloader" Click="RebootBootloaderMenuItem_OnClick"/>
                                </ContextMenu>
                            </Button.ContextMenu>
                        </Button>

                        <Border Width="1" Background="{StaticResource BorderBrushSoft}" Margin="8,4,16,4"/>
                        <Button x:Name="StartMirrorButton" Content="Start Mirror" Click="StartMirrorButton_OnClick"/>
                        <Button x:Name="StopMirrorButton" Content="Stop" Style="{StaticResource SecondaryButton}" Margin="0" Click="StopMirrorButton_OnClick"/>
                    </StackPanel>
                </Grid>
            </Border>

            <Border DockPanel.Dock="Bottom" Background="#99111C2D" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,16,0,0">
                <TextBlock x:Name="StatusTextBlock" Foreground="{StaticResource SubtleTextBrush}" TextTrimming="CharacterEllipsis"/>
            </Border>

            <Grid>
                <TabControl Margin="0">
                <TabItem Header="Files">
                    <Grid Margin="0,14,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="14">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <Button Content="Up" Style="{StaticResource SecondaryButton}" Click="UpDirectoryButton_OnClick"/>
                                <TextBox x:Name="RemotePathTextBox" Grid.Column="1" Text="/sdcard" Margin="0,0,8,0" KeyDown="RemotePathTextBox_OnKeyDown"/>
                                <Button Grid.Column="2" Content="Open" Style="{StaticResource SecondaryButton}" Click="OpenPathButton_OnClick"/>
                                <Button Grid.Column="3" Content="Refresh" Click="RefreshFilesButton_OnClick"/>
                            </Grid>
                        </Border>

                        <ListView x:Name="FilesListView" Grid.Row="1" Margin="0,12,0,12" MouseDoubleClick="FilesListView_OnMouseDoubleClick" PreviewMouseRightButtonDown="FilesListView_OnPreviewMouseRightButtonDown">
                            <ListView.ContextMenu>
                                <ContextMenu>
                                    <MenuItem Header="Download to Downloads\AndroidManagerSuite" Click="DownloadSelectedFileMenuItem_OnClick"/>
                                    <MenuItem Header="Delete Selected" Click="DeleteSelectedFileMenuItem_OnClick"/>
                                </ContextMenu>
                            </ListView.ContextMenu>
                            <ListView.View>
                                <GridView>
                                    <GridViewColumn Header="Name" Width="470" DisplayMemberBinding="{Binding Name}"/>
                                    <GridViewColumn Header="Type" Width="120" DisplayMemberBinding="{Binding Type}"/>
                                    <GridViewColumn Header="Size" Width="130" DisplayMemberBinding="{Binding Size}"/>
                                    <GridViewColumn Header="Modified" Width="190" DisplayMemberBinding="{Binding Modified}"/>
                                </GridView>
                            </ListView.View>
                        </ListView>

                        <StackPanel Grid.Row="2" Orientation="Horizontal">
                            <Button Content="Push File" Click="PushFileButton_OnClick"/>
                            <Button Content="Pull Selected" Style="{StaticResource SecondaryButton}" Click="PullFileButton_OnClick"/>
                            <Button Content="Delete Selected" Style="{StaticResource DangerButton}" Click="DeleteFileButton_OnClick"/>
                        </StackPanel>
                    </Grid>
                </TabItem>

                <TabItem Header="Apps">
                    <Grid Margin="0,14,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="16" Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox x:Name="ApkPathTextBox" IsReadOnly="True" TextChanged="ApkPathTextBox_OnTextChanged"/>
                                <Button Grid.Column="1" Content="Browse APK" Style="{StaticResource SecondaryButton}" Margin="8,0,8,0" Click="BrowseApkButton_OnClick"/>
                                <Button x:Name="InstallApkButton" Grid.Column="2" Content="Install / Update" IsEnabled="False" Click="InstallApkButton_OnClick"/>
                            </Grid>
                        </Border>

                        <Grid Grid.Row="1" Margin="0,0,0,12">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="PackageFilterTextBox" ToolTip="Filter packages" TextChanged="PackageFilterTextBox_OnTextChanged"/>
                            <Button Grid.Column="1" Content="Refresh Packages" Style="{StaticResource SecondaryButton}" Margin="8,0,8,0" Click="RefreshPackagesButton_OnClick"/>
                            <Button Grid.Column="2" Content="Uninstall Selected" Style="{StaticResource DangerButton}" Click="UninstallPackageButton_OnClick"/>
                        </Grid>

                        <ListBox x:Name="PackagesListBox" Grid.Row="2"/>
                    </Grid>
                </TabItem>

                <TabItem Header="Performance &amp; Battery">
                    <Grid Margin="0,14,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>

                        <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,14">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <TextBlock Text="Live Device Performance" FontSize="20" FontWeight="SemiBold"/>
                                    <TextBlock x:Name="PerformanceUpdatedTextBlock" Text="Refresh to load CPU, memory, storage, and process metrics." Foreground="{StaticResource SubtleTextBrush}"/>
                                </StackPanel>
                                <CheckBox x:Name="AutoRefreshCheckBox" Grid.Column="1" Content="Auto Refresh 2s" Foreground="{StaticResource SubtleTextBrush}" VerticalAlignment="Center" Margin="0,0,12,0" Checked="AutoRefreshCheckBox_OnChanged" Unchecked="AutoRefreshCheckBox_OnChanged"/>
                                <Button Grid.Column="2" Content="Refresh" Click="RefreshPerformanceButton_OnClick"/>
                            </Grid>
                        </Border>

                        <UniformGrid Grid.Row="1" Columns="4" Margin="0,0,0,14">
                            <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,10,0">
                                <controls:DonutGauge x:Name="CpuGauge" Height="160" Title="CPU" Detail="total busy" AccentBrush="{StaticResource AccentBrush}"/>
                            </Border>
                            <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,10,0">
                                <controls:DonutGauge x:Name="MemoryGauge" Height="160" Title="Memory" Detail="used" AccentBrush="{StaticResource AccentPurpleBrush}"/>
                            </Border>
                            <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,10,0">
                                <controls:DonutGauge x:Name="StorageGauge" Height="160" Title="Data Storage" Detail="used" AccentBrush="#3EF6A4"/>
                            </Border>
                            <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="12" Margin="0,0,0,0">
                                <StackPanel VerticalAlignment="Center">
                                    <controls:DonutGauge x:Name="BatteryGauge" Height="160" Title="Battery" Detail="" AccentBrush="#FFC107"/>
                                </StackPanel>
                            </Border>
                        </UniformGrid>

                        <Grid Grid.Row="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="370"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,14,0">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <StackPanel>
                                        <TextBlock Text="Load Average" Foreground="{StaticResource SubtleTextBrush}"/>
                                        <Border Background="{StaticResource SurfaceAltBrush}" CornerRadius="6" Padding="10,6" Margin="0,8,0,14">
                                            <TextBlock x:Name="LoadAverageTextBlock" Text="-" FontSize="20" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                        </Border>
                                        <TextBlock Text="Memory" Foreground="{StaticResource SubtleTextBrush}"/>
                                        <TextBlock x:Name="MemoryDetailTextBlock" Text="-" FontSize="14" Margin="0,6,0,14" TextWrapping="Wrap"/>
                                        <TextBlock Text="Storage" Foreground="{StaticResource SubtleTextBrush}"/>
                                        <TextBlock x:Name="StorageDetailTextBlock" Text="-" FontSize="14" Margin="0,6,0,14" TextWrapping="Wrap"/>
                                        <TextBlock Text="Battery" Foreground="{StaticResource SubtleTextBrush}"/>
                                        <UniformGrid Columns="2" Margin="0,6,0,0">
                                            <TextBlock x:Name="BatteryStatusTextBlock" FontSize="13" TextWrapping="Wrap" Margin="0,0,8,6"/>
                                            <TextBlock x:Name="BatteryHealthTextBlock" FontSize="13" TextWrapping="Wrap" Margin="0,0,0,6"/>
                                            <TextBlock x:Name="BatteryTemperatureTextBlock" FontSize="13" TextWrapping="Wrap" Margin="0,0,8,6"/>
                                            <TextBlock x:Name="BatteryVoltageTextBlock" FontSize="13" TextWrapping="Wrap" Margin="0,0,0,6"/>
                                            <TextBlock x:Name="BatteryTechTextBlock" FontSize="13" TextWrapping="Wrap" Margin="0,0,8,0"/>
                                            <TextBlock x:Name="BatteryCapturedTextBlock" Foreground="{StaticResource SubtleTextBrush}" FontSize="13" TextWrapping="Wrap"/>
                                        </UniformGrid>
                                    </StackPanel>
                                </ScrollViewer>
                            </Border>
                            <Border Grid.Column="1" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8" Padding="14">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>
                                    <TextBlock Text="Top Processes" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                                    <ListView x:Name="ProcessesListView" Grid.Row="1">
                                        <ListView.View>
                                            <GridView>
                                                <GridViewColumn Header="PID" Width="90" DisplayMemberBinding="{Binding Pid}"/>
                                                <GridViewColumn Header="Process" Width="360" DisplayMemberBinding="{Binding Name}"/>
                                                <GridViewColumn Header="CPU" Width="110" DisplayMemberBinding="{Binding Cpu}"/>
                                                <GridViewColumn Header="Memory" Width="110" DisplayMemberBinding="{Binding Memory}"/>
                                            </GridView>
                                        </ListView.View>
                                    </ListView>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                </TabItem>
            </TabControl>
            </Grid>
        </DockPanel>

        <Grid x:Name="DialogOverlay" Grid.RowSpan="2" Visibility="Collapsed" Background="#B0000711">
            <Border Width="420" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="10" Padding="22" HorizontalAlignment="Center" VerticalAlignment="Center">
                <StackPanel>
                    <TextBlock x:Name="DialogTitleTextBlock" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    <TextBlock x:Name="DialogMessageTextBlock" Foreground="{StaticResource SubtleTextBrush}" TextWrapping="Wrap" LineHeight="20" Margin="0,0,0,12"/>
                    <TextBox x:Name="DialogInputTextBox" Visibility="Collapsed" Margin="0,0,0,22"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button x:Name="DialogCancelButton" Content="Cancel" Style="{StaticResource SecondaryButton}" Click="DialogCancelButton_OnClick"/>
                        <Button x:Name="DialogOkButton" Content="OK" Click="DialogOkButton_OnClick"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <Grid x:Name="ImagePreviewOverlay" Grid.RowSpan="2" Visibility="Collapsed" Background="#D8000711">
            <Border Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" CornerRadius="10" Padding="16" Width="860" Height="620" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock x:Name="ImagePreviewTitleTextBlock" Text="Image Preview" FontSize="18" FontWeight="SemiBold"/>
                            <TextBlock x:Name="ImagePreviewPathTextBlock" Foreground="{StaticResource SubtleTextBrush}" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal">
                            <Button Content="Fit" Style="{StaticResource SecondaryButton}" Click="ImagePreviewFitButton_OnClick"/>
                            <Button Content="Actual Size" Style="{StaticResource SecondaryButton}" Click="ImagePreviewActualSizeButton_OnClick"/>
                            <Button Content="Close" Style="{StaticResource SecondaryButton}" Click="ImagePreviewCloseButton_OnClick"/>
                        </StackPanel>
                    </Grid>
                    <Border Grid.Row="1" Background="#120B20" BorderBrush="{StaticResource BorderBrushSoft}" BorderThickness="1" CornerRadius="8">
                        <ScrollViewer x:Name="ImagePreviewScrollViewer" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                            <Image x:Name="ImagePreviewControl" Stretch="Uniform" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="12"/>
                        </ScrollViewer>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <Grid x:Name="BusyOverlay" Grid.RowSpan="2" Visibility="Collapsed" Background="#90000711">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Foreground="{StaticResource AccentBrush}" Background="{StaticResource SurfaceBrush}" BorderThickness="0"/>
                <TextBlock Text="Working..." Foreground="{StaticResource TextBrush}" FontSize="14" HorizontalAlignment="Center" Margin="0,12,0,0"/>
            </StackPanel>
        </Grid>

        <Border x:Name="ToastBorder" Grid.RowSpan="2" Visibility="Collapsed" Background="#F7F3FF" BorderBrush="{StaticResource AccentBrush}" BorderThickness="1" CornerRadius="8" Padding="16" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,22,22">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock x:Name="ToastTitleTextBlock" Text="Download complete" Foreground="#1B102D" FontWeight="SemiBold" FontSize="15"/>
                    <TextBlock x:Name="ToastMessageTextBlock" Foreground="#5B4B73" Margin="0,4,0,0" MaxWidth="420" TextWrapping="Wrap"/>
                </StackPanel>
                <Button x:Name="ToastOpenButton" Grid.Column="1" Content="Open" Margin="14,0,0,0" Click="ToastOpenButton_OnClick"/>
            </Grid>
        </Border>
    </Grid>
</Window>
